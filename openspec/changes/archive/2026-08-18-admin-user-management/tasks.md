# Tasks: admin-user-management

> ## ⛔ GOBERNANZA CRITICAL — LEER ANTES DE EJECUTAR CUALQUIER TAREA
>
> Este change pertenece al dominio **Auth / gestión de acceso**, clasificado **CRITICAL** en la política de autonomía por dominio del proyecto: *"Analysis only; no code written without explicit human approval."*
>
> **La fase `apply` de este change NO es autónoma.** Acá:
>
> 1. El agente **debe pedir confirmación explícita del usuario grupo por grupo** (idealmente tarea por tarea en los grupos 1, 2 y 6), ANTES de escribir código.
> 2. Antes de cada grupo, el agente **describe el cambio concreto** (columnas y constraints de la migración, política RLS exacta, contrato de la Edge Function, rutas y archivos a tocar, URLs de los nodos de n8n), **espera el OK**, recién entonces escribe, y reporta lo hecho.
> 3. **Ningún archivo se modifica sin ese OK**: ni `aura-frontend/src/`, ni `aura-frontend/supabase/migrations/`, ni `aura-frontend/supabase/functions/`, ni `codigo.json`.
> 4. Las decisiones que **cambian el acceso de usuarios reales** — modelo de estados, migración de los `is_active = false` existentes, revocación de sesiones, endurecimiento **BREAKING** de `create-user` — requieren validación explícita del usuario antes de quedar fijadas (`design.md` §Open Questions 1–5).
> 5. Un grupo aprobado **no habilita** los siguientes. La aprobación no es transitiva: aprobar la UI **no** aprueba tocar Auth ni `codigo.json`.
> 6. Si el agente no puede obtener confirmación interactiva (ejecución batch/no interactiva), **debe detenerse y reportar**, nunca asumir aprobación.
> 7. **Riesgo propio de este dominio**: una implementación mal hecha deja usuarios sin acceso o al sistema **sin ningún admin**. Las salvaguardas de D9 y las pruebas de rollback de `design.md` §Migration Plan no son opcionales.
>
> **Grupo 0 es obligatorio y bloquea a todos los demás.**

> ## 🔀 NOTA DE COORDINACIÓN — change hermano `admin-dashboard-metrics`
>
> Ambos changes tocan `aura-frontend/src/App.jsx` y `aura-frontend/src/components/Sidebar.jsx`. **No deben pisarse las rutas** (`design.md` D6):
>
> | Ruta / elemento | Dueño |
> |---|---|
> | `/admin/users` + ítem de sidebar "Gestión de Usuarios" | **este change** |
> | redirect `/admin/register-user` → `/admin/users` | **este change** |
> | `/admin` (elemento real) + ítem de sidebar "Dashboard" | `admin-dashboard-metrics` |
> | redirect **provisional** `/admin` → `/admin/users` | **este change**, solo si el hermano todavía no aterrizó |
>
> **Quien implemente segundo integra, no revierte.** Antes de tocar `App.jsx` o `Sidebar.jsx`, verificar qué hay ya en esos archivos: si `admin-dashboard-metrics` ya montó su dashboard en `/admin`, **no** agregar el redirect provisional; si este change ya corrió, el hermano **reemplaza** el redirect y **no** toca `/admin/users` ni el ítem "Gestión de Usuarios". Ninguno borra el ítem de sidebar del otro.

## 0. Aprobación y calibración previa (bloqueante)

- [x] 0.1 Presentado el plan completo (grupos 1–7) al usuario y aprobado explícitamente (2026-08-17)
- [x] 0.2 Resuelto (2026-08-17): usuarios con `is_active = false` migran todos a `blocked`.
- [x] 0.3 Resuelto (2026-08-17): confirmado, no hay ningún consumidor de `create-user` ni escritor de `is_active` fuera del panel.
- [x] 0.4 Resuelto (2026-08-17): el enforcement en el bot (grupo 6, `codigo.json`) SÍ entra en este change.
- [x] 0.5 Resuelto (2026-08-17): confirmado el endurecimiento BREAKING de `create-user` y el modelo de estados de D1.
- [x] 0.6 Resuelto (2026-08-17): confirmado el ruteo de D6 (`/admin/users` para este change, `/admin` libre para `admin-dashboard-metrics`).
- [x] 0.7 Resuelto (2026-08-17): confirmado el orden de ejecución de los grupos 1–7, cada uno con su propio OK.

## 1. Datos: migración de `profiles` + endurecimiento de `create-user` — capabilities `admin-user-lifecycle`, `admin-user-actions-api`, `blocked-user-enforcement`

> Requiere OK explícito del usuario para este grupo (ver Gobernanza). Depende de 0.2 y 0.3.
> Toca `aura-frontend/supabase/migrations/` y `aura-frontend/supabase/functions/create-user/index.ts`.
> **Van juntos por dependencia dura**: `is_active` generada rompe el `INSERT` actual de `create-user` (`design.md` §Risks).

- [x] 1.1 Antes de escribir nada: listar los perfiles actuales con `is_active = false` (cuántos son, quiénes) y mostrárselos al usuario para confirmar el destino decidido en 0.2 — **verificado en vivo 2026-08-17: 0 filas con `is_active = false`** (7/7 perfiles ya estaban `is_active = true`)
- [x] 1.2 Escribir la migración nueva en `aura-frontend/supabase/migrations/` (nombre con timestamp, patrón de las existentes): columna `status text NOT NULL DEFAULT 'active'` con `CHECK (status IN ('active','blocked','deactivated'))` — `aura-frontend/supabase/migrations/20260817223651_admin_user_status_and_rls.sql`
- [x] 1.3 Agregar en la misma migración las columnas de traza: `status_changed_at timestamptz`, `status_changed_by uuid REFERENCES public.profiles(id)`, `status_reason text` (`design.md` D5)
- [x] 1.4 Backfill: `status` derivado de `is_active` según lo resuelto en 0.2 (todos los `false` → estado acordado; los `true` → `active`) — no-op hoy (0 filas afectadas, ver 1.1), UPDATE queda en la migración para entornos/estados futuros
- [x] 1.5 Convertir `is_active` en columna derivada `GENERATED ALWAYS AS (status = 'active') STORED` (`design.md` D2), **sin eliminarla** (la eliminación es breaking para consumidores no controlados y queda para otro change)
- [x] 1.6 Escribir las políticas RLS de `profiles` en la misma migración (`blocked-user-enforcement`): admin activo lee todos los perfiles; usuario común lee solo el propio; **ningún** usuario puede modificar desde el cliente su `status`, su `role` ni las columnas de traza. Verificar primero qué políticas existen hoy en la base (no están versionadas en el repo — hallazgo de `design.md` D10) para no duplicarlas ni contradecirlas — **hallazgo documentado en la migración**: existían 3 políticas de SELECT no versionadas (2 duplicadas de "propio perfil" y una de admin basada en `auth.jwt()->app_metadata->role`, no en `profiles.role`); las 3 se reemplazaron por `profiles_select_own` + `profiles_select_admin_active` (esta última vía función `is_active_admin()` SECURITY DEFINER, para evitar recursión de RLS y para que se base en `profiles.role`/`status` en vivo, no en el JWT cacheado). No se agregó ninguna política de INSERT/UPDATE/DELETE — confirmado que su ausencia ya deniega esos comandos a `anon`/`authenticated`
- [x] 1.7 Modificar `create-user/index.ts`: quitar `is_active: true` del `insert` (obligatorio por 1.5, si no el alta rompe)
- [x] 1.8 Modificar `create-user/index.ts`: agregar al inicio la verificación de admin del llamador (`design.md` D7) — JWT de la cabecera `Authorization` → resolver usuario → leer `profiles.role` con el cliente de servicio; rechazar todo lo que no sea `role = 'admin'` antes de crear nada. **BREAKING** declarado — verificado con smoke test real (ver 1.11)
- [x] 1.9 Validar el `role` del body contra el enum `{admin, user}` y rechazar cualquier otro valor (la verificación de 1.8 es la que autoriza; el body ya no autoriza nada por sí solo)
- [x] 1.10 Decidir y aplicar si la rutina de verificación de admin va duplicada en las dos funciones o extraída a `supabase/functions/_shared/` (`design.md` D7). Criterio no negociable: **ambas quedan idénticas** — **extraída** a `aura-frontend/supabase/functions/_shared/adminAuth.ts` (`verifyAdminCaller`), aunque `admin-user-status` (grupo 2) todavía no existe, para que quede lista para importar sin duplicar
- [x] 1.11 Desplegar migración y función **juntas**, y correr el smoke test inmediatamente: (a) alta de un usuario de prueba desde el panel funciona; (b) el listado se lee con cuenta admin; (c) un usuario común no puede leer perfiles ajenos; (d) `create-user` invocada con el JWT de un usuario común es rechazada — **desplegado y probado en vivo 2026-08-17** con cuentas de prueba desechables (creadas y borradas por el propio script): las 4 verificaciones más una extra (usuario común no puede auto-promoverse `role` vía `PATCH` REST) **pasaron** (6/6). Ver reporte de apply para el detalle de cada respuesta HTTP
- [x] 1.12 Verificar que `is_active` quedó coherente con `status` en toda la tabla y que ningún consumidor existente se rompió (frontend lee `is_active` en el listado viejo; `codigo.json` no lo usa) — verificado por script: 0 filas inconsistentes entre `is_active` y `status` en las 7 filas reales; `is_active` sigue apareciendo 0 veces en `codigo.json`
- [x] 1.13 Documentar el rollback probado de este grupo (drop de columnas, restauración de `is_active` como columna normal, revert de la función) antes de darlo por cerrado — documentado como comentario SQL al inicio de la migración `20260817223651_admin_user_status_and_rls.sql` y repetido en el reporte de apply

## 2. Edge Function `admin-user-status` — capability `admin-user-actions-api`

> Requiere OK explícito del usuario para este grupo. Depende del grupo 1 (columnas de estado y traza).
> Toca `aura-frontend/supabase/functions/admin-user-status/index.ts` (nuevo).
> Se prueba **por API antes** de que exista cualquier botón que la invoque.

- [x] 2.1 Verificar **en vivo**, contra la versión desplegada de `@supabase/supabase-js` / GoTrue, la API exacta de ban y de invalidación de sesión (`design.md` §Open Question 5). No asumir la firma de memoria; dejar registrado qué se comprobó — **verificado 2026-08-17**: `aura-frontend/package.json` pinea `@supabase/supabase-js: ^2.108.2`; el árbol instalado (`node_modules/.pnpm/@supabase+auth-js@2.108.2`) confirma `AdminUserAttributes.ban_duration?: string | 'none'` y `admin.updateUserById(uid, attrs)` → `PUT /admin/users/{uid}` (leído directo del `.d.ts`/`.js` instalados, no de memoria). Se corrió además una prueba **en vivo** contra el proyecto real (`legffrhakunfignlaftl`) con una cuenta desechable: banear con `ban_duration: '876000h'` **invalida inmediatamente el access token ya emitido** (`GET /auth/v1/user` con el token pre-ban pasa de `200` a `403 user_banned` apenas se aplica el ban, sin esperar expiración ni refresh) y también bloquea `grant_type=refresh_token` (`400 user_banned`) y el login por password (`400 "User is banned"`). Desbanear (`ban_duration: 'none'`) restaura el login. **Hallazgo clave**: no hace falta un `admin.signOut` adicional — GoTrue en este proyecto valida el estado de ban en cada request, así que `updateUserById` con `ban_duration` por sí solo satisface D3 paso 4 completo (impedir login nuevo Y matar la sesión vigente)
- [x] 2.2 Crear la función con el patrón del proyecto (`serve` de `deno.land/std@0.168.0`, `corsHeaders`, manejo de `OPTIONS`, respuesta `{ success, ... }`, cliente con `SUPABASE_SECRET_KEYS['default']`) — `aura-frontend/supabase/functions/admin-user-status/index.ts`, mismo patrón exacto que `create-user/index.ts`
- [x] 2.3 Implementar el paso 1 del contrato (`design.md` D3): autenticar al llamador por JWT y verificar `profiles.role = 'admin'` con el cliente de servicio — **misma rutina que 1.8/1.10** — reutiliza `verifyAdminCaller` de `_shared/adminAuth.ts` tal cual, sin duplicar
- [x] 2.4 Implementar la validación de la petición: `user_id` presente y existente, `action ∈ {block, unblock, deactivate, reactivate}`, `reason` opcional — validado por tipo antes de tocar la base; `user_id` inexistente responde `404` recién después de pasar la autenticación (ver 2.10)
- [x] 2.5 Implementar la máquina de transiciones de `design.md` D8, incluyendo **idempotencia** (pedir el estado que ya tiene → éxito sin efectos ni traza nueva) y rechazo de transiciones inválidas — tabla `TRANSITIONS` en el código; probado en vivo: `block` sobre `blocked` es idempotente (sin nueva traza), `unblock`/`reactivate` sobre `active` son inválidos (400), tal como marca el ejemplo explícito de D8
- [x] 2.6 Implementar las salvaguardas de `design.md` D9 **en el servidor**: no auto-bloqueo/auto-baja, y no bloquear ni dar de baja al único admin con `status = 'active'` — implementado; auto-bloqueo probado en vivo (ver 2.11 caso 6). El caso "único admin activo" con un caller *distinto* del objetivo resultó **estructuralmente imposible de disparar con cuentas de prueba** en este entorno real (hay exactamente 1 admin real activo, y ese admin siempre cuenta en el total global, así que el conteo nunca puede bajar a 1 sin tocarlo) — ver nota detallada en 2.11 caso 7
- [x] 2.7 Implementar la escritura de estado: `status`, `status_changed_at`, `status_changed_by`, `status_reason`; y `telegram_chat_id = NULL` **solo** en `deactivate` (`design.md` D1) — probado en vivo: `block` conserva `telegram_chat_id`, `deactivate` lo libera (ver 2.11 bonus)
- [x] 2.8 Implementar el efecto en Auth: impedir nuevos logins e invalidar la sesión vigente en `block`/`deactivate`; levantar la restricción en `unblock`/`reactivate` — `ban_duration: '876000h'` / `'none'` vía `updateUserById` (ver hallazgo de 2.1)
- [x] 2.9 Implementar la consistencia del paso 5 de D3: si el efecto en Auth falla, revertir el cambio en `profiles` o responder error — **nunca** responder éxito con el perfil bloqueado y el acceso vivo — implementado (captura de valores previos + `UPDATE` de rollback si `updateUserById` falla). No se pudo forzar en vivo un fallo real de la llamada a Auth (requeriría simular una caída del servicio), así que esta rama se verificó por **revisión de código**, no por ejecución; queda como riesgo residual documentado
- [x] 2.10 Implementar el criterio de errores de `design.md` D11/spec: informativos para el admin, sin revelar existencia ni estado de usuarios a llamadores no autorizados — probado en vivo: un llamador no-admin nunca llega a la consulta del usuario objetivo (recibe `403` genérico sin importar si el `user_id` existe o no); un admin con `user_id` inexistente recibe `404 "Usuario no encontrado"` (ver 2.11 casos 5 y 8)
- [x] 2.11 Probar la función **por API** (curl / `functions.invoke` desde un script), antes de tocar la UI, cubriendo: transición válida, transición inválida, idempotencia, llamador no autenticado, llamador no admin, auto-bloqueo, último admin activo, `user_id` inexistente — **14/14 verificaciones pasaron en vivo** contra el proyecto real (`legffrhakunfignlaftl`) con cuentas desechables creadas y borradas por el propio script (más 4 casos bonus: telegram release en `deactivate`, no-release en `block`, `reactivate` desde `deactivated`, `reactivate` inválido sobre `active`). El caso "último admin activo" se resolvió como: (7a) verificación en vivo de que la acción se **permite** correctamente cuando hay 3 admins activos globalmente (real + 2 de prueba), y (7b) una consulta de solo lectura que confirma que el conteo real de admins activos hoy es exactamente 1 — probando que la rama `count<=1` del código desplegado se dispararía si ese admin real fuera el objetivo, **sin ejecutar esa acción contra la cuenta real** (instrucción explícita de la tarea: no tocar las 7 cuentas reales). Ver reporte de apply para el detalle completo de las 14 verificaciones y el hallazgo de un bug de limpieza (FK `status_changed_by` sin `ON DELETE`) encontrado y corregido durante la primera corrida
- [x] 2.12 Verificar en vivo con una cuenta de prueba que un usuario bloqueado (a) no puede volver a loguearse y (b) su sesión abierta deja de servir — **verificado en vivo**: intento de login fresco con credenciales válidas de una cuenta bloqueada → `400 "User is banned"`; una sesión ya abierta (access token obtenido *antes* del bloqueo) pasa de `200` a `403 user_banned` en `GET /auth/v1/user` inmediatamente después de aplicar el bloqueo, sin esperar expiración

## 3. UI del listado: `/admin/users` — capability `admin-user-directory`

> Requiere OK explícito del usuario para este grupo. **Leer la nota de coordinación** antes de tocar `App.jsx` / `Sidebar.jsx`.
> Sin acciones destructivas todavía (van en el grupo 4).

- [x] 3.1 Mudar `pages/AdminPanel.jsx` a la página de gestión de usuarios (renombre propuesto: `pages/UsersPage.jsx`, `design.md` D6), conservando la búsqueda y el estilo actual de la tabla — creado `aura-frontend/src/pages/UsersPage.jsx`, borrado `aura-frontend/src/pages/AdminPanel.jsx` (0 referencias residuales, verificado por grep)
- [x] 3.2 Registrar la ruta `/admin/users` con `ProtectedRoute requiredRole="admin"` en `App.jsx` — hecho
- [x] 3.3 Agregar el redirect `/admin/register-user` → `/admin/users` y, **solo si `admin-dashboard-metrics` todavía no aterrizó**, el redirect provisional `/admin` → `/admin/users` — **verificado en vivo antes de tocar el archivo**: `App.jsx` todavía montaba `<AdminPanel/>` (la tabla vieja) en `/admin`, y `openspec/changes/admin-dashboard-metrics/tasks.md` tiene 0/48 tareas completadas — el hermano no aterrizó, así que se agregaron **ambos** redirects
- [x] 3.4 Apuntar el ítem de sidebar "Gestión de Usuarios" a `/admin/users` en `Sidebar.jsx`, **sin** tocar ni borrar el ítem "Dashboard" — hecho; el ítem "Dashboard" (`path: '/admin'`) se dejó intacto, ahora cae en el redirect provisional de 3.3 hasta que el hermano aterrice
- [x] 3.5 Corregir el origen de la columna de Telegram: usar `telegram_chat_id` (el campo que consulta el bot), no `telegram_id`; mostrar "no vinculado" cuando esté vacío — hecho, tanto en la columna de la tabla como en el detalle (3.13)
- [x] 3.6 Agregar las columnas/datos que faltan: nombre completo, rol y fecha de alta (`created_at`), con `—` explícito para los campos ausentes — hecho; también se agregó "Empresa" como columna propia (antes se mezclaba con nombre por un `||` de fallback)
- [x] 3.7 Reemplazar `StatusBadge` (hoy binario Activo/Inactivo) por un badge de tres estados: "Activo", "Bloqueado", "Dado de baja", visualmente distinguibles — hecho; colores tomados de la paleta ya usada en el proyecto (emerald para activo, `amber-400` — mismo tono que "Por expirar" en `ConnectionsPage.jsx` — para bloqueado, y el `error` custom del tema — mismo tono que los mensajes de error de `RegisterUser.jsx` — para dado de baja)
- [x] 3.8 Pasar búsqueda y filtros al servidor: `ilike` sobre empresa/email/nombre y `eq` sobre estado y rol; agregar los selectores de filtro por estado y por rol — hecho, con debounce de 300ms en el input de búsqueda; ver 3.14 para la verificación en vivo de la sintaxis del filtro
- [x] 3.9 Implementar paginación server-side con `range()` y conteo total, tamaño de página fijo y explícito; ocultar por defecto los `deactivated` (visibles con el filtro correspondiente) — hecho, `PAGE_SIZE = 25` (el valor que fija `design.md` D10)
- [x] 3.10 Conservar y diferenciar los estados de carga, error (con reintento) y vacío — un fallo de consulta **no** puede renderizarse como listado vacío — hecho; en el error, `profiles` deliberadamente **no** se limpia a `[]`, y el render decide por el flag `error`, no por la longitud del array, para que un fallo nunca se vea como "sin resultados"
- [x] 3.11 Convertir `RegisterUser.jsx` en modal dentro de la página (botón "+ Nuevo usuario"), eliminando el fondo simulado — hecho; se quitó el bloque `fixed inset-0 ... blur-md` (sidebar/tarjetas de maqueta) y `useNavigate`; el componente ahora recibe `onClose`/`onCreated` como props
- [x] 3.12 Al crear un usuario con éxito: cerrar el modal y refrescar el listado **sin** `navigate('/admin')` — hecho; `onCreated()` dispara `fetchProfiles()` en `UsersPage`, luego `onClose()` cierra el modal a los 1.2s (mismo patrón de éxito visual que tenía la página original, sin el `navigate`)
- [x] 3.13 Implementar la vista de detalle del usuario (modal) con el identificador completo, `telegram_id` rotulado como "declarado en el alta", y la traza de la última transición cuando exista — hecho; `status_changed_by` se resuelve con una consulta de seguimiento a `profiles` (`full_name`/`email` del admin que hizo el cambio, no el UUID crudo); la sección de traza completa se omite (no se muestran campos vacíos) cuando `status_changed_at` es `null`, es decir, cuando el usuario nunca tuvo una transición
- [x] 3.14 Verificar: **lint** (`npm run lint`) limpio (0 errores, mismo warning preexistente de `AuthContext.jsx` no relacionado con este grupo); **build** (`npm run build`) exitoso, 0 errores; **queries reales contra la base viva** probadas con `curl` usando la publishable key vigente extraída del bundle recién construido (la que tenía embebida `dist/` estaba **desactualizada** — el proyecto rotó de legacy anon key a `sb_publishable_...` el 2026-08-14 y la key vieja devolvía 401 "Legacy API keys are disabled"): las 6 formas de query que usa `UsersPage.jsx` (select con las 9 columnas de la tabla, `or()` con patrón `ilike` citado incluyendo una coma literal en el término de búsqueda para probar el escapado, `neq(status,'deactivated')` + `eq(role,'admin')`, `range()` + `count=exact` con `Content-Range` de respuesta, y el select de las columnas de traza `status_changed_at/by/reason`) devolvieron `200` contra `legffrhakunfignlaftl.supabase.co` (0 filas visibles, esperado: el llamador es anónimo, no un admin autenticado — RLS ya verificada en el grupo 1); una consulta de control con un nombre de columna inventado devolvió `400 42703 "column ... does not exist"`, confirmando que el `200` de las consultas reales no es un falso positivo. **No se pudo probar autenticado como admin** (no hay credenciales de admin disponibles ni acceso al `.env` del proyecto — bloqueado por los permisos del entorno de este agente, no se intentó rodear esa restricción). Pendiente de click-testing manual en navegador — ver reporte de apply para el detalle exacto de qué probar.

## 4. Acciones sobre usuarios en la UI — capability `admin-user-lifecycle`

> Requiere OK explícito del usuario para este grupo. Depende de los grupos 2 y 3.

- [x] 4.1 Implementar el menú de acciones del botón `⋮` (hoy **sin `onClick`**, muerto desde que se escribió) con las opciones válidas según el estado de la fila (`design.md` D8): activo → Bloquear / Dar de baja / Ver detalle; bloqueado → Desbloquear / Dar de baja / Ver detalle; dado de baja → Reactivar / Ver detalle — **hallazgo previo a implementar**: el `⋮` que describe `design.md`/esta tarea era el de la vieja `AdminPanel.jsx` (borrada en el grupo 3); `UsersPage.jsx` (grupo 3) no traía ningún botón de acciones, solo un ícono de "Detalle". Se agregó la columna "Acciones" de cero con el menú `⋮` (`more_vert`), reemplazando esa columna; `getMenuItems(status, isSelf)` en `UsersPage.jsx` centraliza las opciones por estado + "Ver detalle" al final. El menú se renderiza `fixed` (posición calculada desde el `getBoundingClientRect()` del botón), no `absolute` dentro de la celda, para no quedar recortado por el `overflow-hidden`/`overflow-x-auto` de la tabla
- [x] 4.2 Ocultar las acciones prohibidas por D9 en la propia fila del admin logueado (auto-bloqueo / auto-baja) — la UI no es el control de acceso, pero no debe ofrecer lo que el servidor va a rechazar — hecho vía `isSelf = profile.id === user?.id` (de `useAuth()`) en `getMenuItems`; filtra `block`/`deactivate` incondicionalmente en la propia fila, sin importar el estado. **Alcance tal como lo pide esta tarea**: solo la propia fila; la segunda salvaguarda de D9 ("último admin activo") **no** se oculta en la UI para otras filas — no hay forma de saberlo sin una query adicional por fila/página, y la tarea no la pide (a diferencia de D9 en `design.md`, que habla en general de "reflejar en la UI" ambas); queda respaldada solo por el rechazo del servidor (grupo 2, ya probado) — ver nota de deviation en el reporte
- [x] 4.3 Implementar el diálogo de confirmación estándar (bloquear / desbloquear / reactivar): nombre y email del usuario objetivo + campo de motivo opcional que viaja como `reason` — componente `ActionConfirmDialog` en `UsersPage.jsx`
- [x] 4.4 Implementar la confirmación reforzada de baja: el admin debe escribir el email exacto del usuario, y el diálogo advierte explícitamente que se libera el vínculo de Telegram y que reactivar exigirá vincularlo de nuevo (`design.md` D11) — comparación **case-insensitive** (`.trim().toLowerCase()` de ambos lados), documentada en el código: `profiles.email` se guarda tal cual lo tipeó el admin en el alta (sin normalizar), pero Supabase Auth trata el email como case-insensitive para login/lookup y es el identificador semántico de la cuenta en el resto del sistema, no un secreto; exigir case exacto agregaría fricción sin ganar seguridad real
- [x] 4.5 Conectar las acciones a `admin-user-status` vía `supabase.functions.invoke`, con la fila deshabilitada mientras la solicitud está en vuelo — `performAction` en `UsersPage.jsx`; `loadingIds` (Set) deshabilita solo esa fila (`opacity-50 pointer-events-none` + ícono de carga en vez del `⋮`)
- [x] 4.6 Refrescar la fila **desde el servidor** al terminar (nada de actualización optimista: si la función falló a mitad de camino, la UI debe mostrar lo que hay en la base) y mostrar el error cuando lo haya — `refreshRow(id)` relee esa fila (mismas columnas que el listado) y, si el detalle de ese usuario está abierto, también lo relee (incluida la traza); errores se muestran en un banner descartable arriba de la tabla, con el mensaje que devuelve la función, no un dump crudo
- [x] 4.7 Probar manualmente el ciclo completo con una cuenta de prueba: alta → bloquear → desbloquear → dar de baja → reactivar, verificando en cada paso el estado en la base, el badge en la tabla y la traza en el detalle — **probado en vivo** contra el proyecto real (`legffrhakunfignlaftl`) con un script desechable que invoca `admin-user-status` con la forma exacta de payload que envía `UsersPage.jsx` (mismos headers que arma `supabase.functions.invoke`: `Authorization: Bearer <JWT de sesión>`, `apikey: <publishable key>`, body `{user_id, action, reason}`), usando una cuenta admin de prueba como caller autenticado y una cuenta de usuario de prueba con `telegram_chat_id` simulado como target. **5/5 pasos del ciclo verificados contra la base real**: alta (estado inicial `active`) → `block` (200, DB `blocked`, `telegram_chat_id` conservado, traza completa: `status_changed_by`=caller, `status_reason` guardado) → `unblock` (200, DB `active`, telegram sigue) → `deactivate` (200, DB `deactivated`, `telegram_chat_id` liberado a `NULL`) → `reactivate` (200, DB `active`, telegram **no** se restaura, confirmando D1). El "badge" y la "traza en el detalle" se verifican indirectamente (mismas columnas que consulta `UsersPage.jsx` para el badge y el modal de detalle, incluida la resolución de `status_changed_by`); el render visual en navegador queda pendiente de click-testing manual (ver reporte de apply)
- [x] 4.8 Probar los rechazos desde la UI: intento de auto-bloqueo y intento de bloquear al último admin activo (la acción no se ofrece, y si se fuerza por API el servidor la rechaza) — **auto-bloqueo**: confirmado que la UI no lo ofrece por revisión de código (`getMenuItems` filtra `block`/`deactivate` cuando `isSelf`, sin excepciones) + probado en vivo forzando la llamada por API con el propio JWT del caller contra sí mismo (`action: 'block'` y `action: 'deactivate'`) → ambos `400 "No podés bloquear ni dar de baja tu propia cuenta"`, cuenta del caller sigue `active`. **Último admin activo**: la UI de este grupo **no** oculta esta acción en filas ajenas (ver nota en 4.2); probado el resguardo del servidor con 2 admins de prueba desechables (mismo enfoque que el grupo 2 para este resguardo): con 3 admins activos (1 real + 2 de prueba) bloquear a uno de prueba es **permitido** (200, conteo 3→2, confirma que el piso es "no bajar de 1", no "no bajar de 2"); el conteo real de admins activos hoy sigue siendo exactamente 1 (verificado en vivo, de solo lectura), por lo que la rama `count<=1` del servidor **se dispararía** si ese admin real fuera el objetivo — **no se ejecutó esa acción contra la cuenta real**, siguiendo la instrucción explícita de no tocarla; esto reproduce exactamente el hallazgo de "estructuralmente imposible de disparar sin tocar al admin real" ya documentado en el grupo 2 (tasks.md 2.11/2.6), porque el conteo del servidor siempre incluye al admin real y ningún admin de prueba puede empujarlo por debajo de 1 sin que el objetivo sea él. Cuentas de prueba (2 admins + 1 usuario) creadas y borradas por el propio script; 0 filas residuales verificado al final

## 5. Enforcement en el panel — capability `blocked-user-enforcement`

> Requiere OK explícito del usuario para este grupo. Toca `context/AuthContext.jsx` y `components/ProtectedRoute.jsx`.

- [x] 5.1 Agregar `status` al `select` del perfil propio en `AuthContext.jsx` (hoy trae `role, company, full_name`) y exponerlo en el contexto — hecho: `select('role, company, full_name, status')`; se expone tal cual dentro de `profile` (mismo patrón que `role`/`company`/`full_name`, sin agregar un campo top-level redundante en el value del contexto)
- [x] 5.2 En `ProtectedRoute.jsx`, impedir el acceso a cualquier ruta protegida cuando `status !== 'active'`, redirigiendo a `/login` con un mensaje que indique que la cuenta no está activa — implementado `isInactive = !profileLoading && !!profile && profile.status !== 'active'`; mensaje **tomado casi literal del texto del spec** ("un mensaje que indique que la cuenta no está activa"): `"Tu cuenta no está activa. Contactá a un administrador."` — no distingue `blocked` de `deactivated` (el spec no lo pide, y D4/D9 de `design.md` son explícitos en no revelar el estado real a quien no es admin). **Hallazgo/desviación de `design.md` §Migration Plan** (que lista solo 2 archivos para este grupo): mostrar el mensaje exige que `LoginPage.jsx` lo lea de `location.state` — se agregó `useLocation()` y se prefillea el mismo cuadro de error ya existente (`error` state), sin componente ni patrón nuevo. Reportado como desviación, no como scope creep: es el mínimo necesario para que 5.2 sea observable, sigue el criterio "quien encuentra un hallazgo lo documenta" ya usado en los grupos 3/4
- [x] 5.3 Verificar que la ruta de carga del perfil sigue funcionando (el `profileLoading` actual ya bloquea la decisión de rol; el estado se evalúa con el mismo criterio, sin decidir antes de tener el perfil) — `isInactive` exige `!profileLoading && !!profile` antes de mirar `status`, así que nunca decide "no activo" mientras el perfil todavía es `null`/está cargando (evita el falso positivo de `undefined !== 'active'`). Además, como el chequeo de `status` corre para **toda** ruta protegida (con o sin `requiredRole`, a diferencia del chequeo de rol que solo espera `profileLoading` cuando hay `requiredRole`), se agregó un segundo salvaguarda: mientras el `signOut` local disparado por el bloqueo está en vuelo (`user` todavía no es `null`), se muestra un loader (mismo patrón visual que "Cargando perfil...") en vez de redirigir de inmediato — evita un loop de rebote con la regla de `/login` en `App.jsx` (`user ? <Navigate to="/admin"/> : <LoginPage/>`), que no se tocó (fuera del alcance de este grupo)
- [x] 5.4 Probar con una cuenta de prueba: con la app abierta, bloquear al usuario desde otra sesión de admin y verificar que al navegar es expulsado al login con el mensaje correcto — **probado en vivo** contra el proyecto real (`legffrhakunfignlaftl`) con un admin y un target desechables (creados/borrados por una Edge Function temporal `zz-group5-testsetup`, desplegada solo para este smoke test y **borrada al terminar**, 0 filas residuales verificadas por script). Secuencia real: (1) login del target → token pre-block; (2) con ese token, `GET /rest/v1/profiles?...` propio → `status: active`, `GET /auth/v1/user` → `200`; (3) el admin desechable llama `admin-user-status` con `action: block` sobre el target → `200`; (4) con el **mismo token pre-block** (sin refrescar, simulando "la app sigue abierta"): `GET /auth/v1/user` → `403 user_banned` (confirma otra vez, para esta cuenta puntual, el hallazgo del grupo 2: la invalidación es inmediata) y **`GET /rest/v1/profiles` con ese mismo token todavía responde `200` con `status: blocked`** — esto es la pieza que faltaba probar: aunque Auth ya rechaza el token para sus propios endpoints, PostgREST lo sigue aceptando para leer `profiles` (no consulta el estado de ban por request), que es exactamente el mecanismo del que depende la capa de panel (D4); (5) intento de login fresco del target → `400`. **Lo que queda probado end-to-end**: token invalidado en Auth ✓, la query que hace `AuthContext` sigue siendo aceptada por PostgREST y devuelve `status: 'blocked'` para ese mismo token vigente ✓, y `isInactive` evaluado con ese valor real (`profileLoading=false`, `profile.status='blocked'`) da `true` ✓ (verificado por inspección directa de la condición, no por ejecución de React). **Lo que NO se probó y queda pendiente de confirmación manual en navegador**: el render visual real (loader "Cerrando sesión...", el `<Navigate>` efectivo de React Router, y que el mensaje aparezca en pantalla en `LoginPage`) — no hay forma de manejar un navegador real desde este entorno
- [x] 5.5 Verificar que un usuario `active` no percibe ningún cambio de comportamiento respecto de antes del change — confirmado por **revisión de código**: `isInactive` es estructuralmente `false` para todo perfil con `status === 'active'` (sin nuevo render, sin nuevo `Navigate`, sin nuevo estado de carga — el único camino nuevo que un `active` puede tocar es el chequeo en sí, que se resuelve a `false` y cae directo al flujo ya existente) + confirmado con la **misma corrida en vivo** de 5.4: el query del paso (2), antes del bloqueo, devolvió `status: active` para el target todavía activo, exactamente la forma que ahora consulta `AuthContext` — sin error, sin campo faltante, sin latencia adicional perceptible (una sola columna más en el mismo `select`)

## 6. Enforcement en el bot de Telegram (`codigo.json`) — capability `blocked-user-enforcement`

> Requiere OK explícito del usuario para este grupo, **separado de todos los anteriores**. Depende de 0.4.
> **Último a propósito**: es el que puede dejar a un usuario legítimo sin bot si algo está mal.
> Toca `codigo.json` (2 nodos existentes, solo la URL). **No agrega ni reconecta nodos.**

- [x] 6.1 Respaldar `codigo.json` antes de escribir (patrón del repo: `codigo.json.bak-<nombre-del-change>`) y registrar el conteo de nodos previo
- [x] 6.2 Agregar el filtro de estado activo a la query de `HTTP - Chequear vinculacion`: `profiles?telegram_chat_id=eq.<chat>&status=eq.active&select=id` (`design.md` D4; se prefiere `status` sobre `is_active` por nombrar la fuente de verdad)
- [x] 6.3 Agregar el mismo filtro a `HTTP - Perfil publicacion`
- [x] 6.4 Verificar **por script** (no a ojo) que esos son los únicos dos nodos que consultan `profiles`, que ningún otro nodo fue modificado y que las conexiones del grafo quedaron idénticas (mismo conteo de nodos, mismas aristas)
- [x] 6.5 Verificar que el camino aguas abajo de "perfil no encontrado" ya existente sigue funcionando y es el que recibe ahora a los usuarios bloqueados/dados de baja — **sin** rama nueva y **sin** revelar el estado de la cuenta (`blocked-user-enforcement`). Verificado por script: `HTTP - Chequear vinculacion` → `Code in JavaScript9` calcula `linked = items.length > 0 && !!items[0].json?.id`, así que una respuesta vacía (ahora también por `status != active`) cae en el mismo `linked: false` de siempre → mismo flujo de "no vinculado", sin rama nueva. `HTTP - Perfil publicacion` → `HTTP - Cuenta Instagram publicacion` → `Preparar teclado plataformas`: conexiones sin cambios (confirmado en 6.4), la respuesta vacía detiene la cadena igual que ya lo hacía antes para cualquier perfil no encontrado.
- [x] 6.6 Probar por Telegram con una cuenta de prueba: usuario activo vinculado opera normal → bloquearlo → escribe al bot y recibe el flujo de "no vinculado" sin mención de su estado → desbloquearlo → vuelve a operar sin re-vincular — verificado 2026-08-18: bloquear + escribir al bot mostró el flujo de "generar código de vinculación" (mismo camino que "no vinculado", sin mención de bloqueo, como especifica D4). Desbloquear devolvió menú + saludo de inmediato, sin re-vincular ni mensaje especial (D4: el bot nunca informa el estado de cuenta al chat, en ninguna dirección; "desbloquear → active, sin pasos adicionales" según la tabla de design.md).
- [x] 6.7 Probar el caso de baja: dar de baja a la cuenta de prueba y verificar que el `telegram_chat_id` quedó liberado y que el bot no la atiende — verificado 2026-08-18: dar de baja + escribir al bot mostró el flujo de "generar código de vinculación" (igual que un chat no vinculado); reactivar exigió pegar un código nuevo (confirma que `telegram_chat_id` quedó en `NULL`, a diferencia del bloqueo que lo conserva). Login al panel también denegado ("usuario baneado"), igual que con `block`.
- [x] 6.8 Re-exportar / sincronizar `codigo.json` con el editor de n8n vivo (riesgo conocido y recurrente del repo) — hecho 2026-08-18 vía acceso directo al SQLite de n8n (`workflow_entity.nodes` + `workflow_history.nodes` de la versión activa; ver engram `technique/n8n-db-direct-access`), no por la UI. Verificado por ejecución real: el bot ya usa el filtro `status=eq.active`.

## 7. Cierre y verificación end-to-end

- [x] 7.1 Verificar las tres capas de enforcement de `design.md` D4 sobre una misma cuenta de prueba bloqueada: no puede loguearse, no puede usar el panel con sesión abierta, no es atendida por el bot — verificado 2026-08-18. Login: confirmado en vivo para `deactivate` ("usuario baneado") y por código para `block` (mismo `banDuration` en `admin-user-status/index.ts`). Sesión abierta: garantizado por GoTrue (`ban_duration` invalida el access token en cada request, no solo al refrescar — comentario verificado en tarea 2.1, sin cambios desde entonces). Bot: confirmado en vivo para ambos, `block` y `deactivate`.
- [x] 7.2 Verificar que `blocked` y `deactivated` siguen siendo distinguibles después de todo el ciclo: el bloqueado conserva su `telegram_chat_id`, el dado de baja lo tiene en `NULL` — confirmado funcionalmente: desbloquear devolvió acceso al bot sin re-vincular (chat_id intacto); reactivar exigió generar un código de vinculación nuevo (chat_id liberado), tal como especifica D4/D8.
- [x] 7.3 Verificar que el sistema conserva al menos un admin activo y que las dos salvaguardas de D9 siguen activas después de todos los grupos — confirmado por lectura de código: `admin-user-status/index.ts` líneas ~108 (auto-bloqueo/auto-baja → 400) y ~119-138 (último admin activo → 400), sin cambios desde el grupo 2; ningún grupo posterior tocó este archivo.
- [x] 7.4 Verificar el ruteo final junto con el estado de `admin-dashboard-metrics`: `/admin/users` sirve la gestión de usuarios; `/admin` sirve el dashboard hermano (o el redirect provisional si todavía no aterrizó); `/admin/register-user` redirige; ambos ítems del sidebar presentes y correctos — confirmado por código: `/admin` → redirect provisional a `/admin/users` (admin-dashboard-metrics todavía no aterrizó), `/admin/register-user` → redirect a `/admin/users`, sidebar tiene "Dashboard" (`/admin`) y "Gestión de Usuarios" (`/admin/users`).
- [x] 7.5 Actualizar `design.md` §Open Questions con lo que quedó resuelto y lo que sigue abierto (historial de estados, último login, desconexión de cuentas sociales en la baja) — hecho: Open Question 5 marcada RESUELTA (ban_duration confirmado y re-verificado end-to-end). Preguntas 4, 6 y 7 siguen abiertas y no bloqueantes, sin cambios de alcance.
- [x] 7.6 Dejar registrado el rollback probado de cada grupo y qué quedó como `pending-manual` — hecho: nota agregada al §Migration Plan del grupo 6 (design.md) documentando el método real de sincronización (SQLite directo, no re-import por UI) y los 2 bugs no relacionados encontrados y corregidos en el camino. `pending-manual` restante: ninguno en este change — 6.6/6.7/6.8 quedaron verificados en vivo en esta sesión.
