# Tasks: link-code-reproducible

> El flujo real de vinculación vive solo en la DB live: la tabla `telegram_link_codes` no está en ninguna migración y el RPC `link_telegram_with_code` no está en el repo. Este cambio lo materializa (migración idempotente, design D1-D2, D7-D8), reconecta la validación `/start <código>` del bot (design D3-D5) y agrega verificación offline (D10). `telegram_link_tokens` se deja intacta (legacy; fuera de scope).
>
> **Regla de oro:** no regresionar `secrets-migration`, `bug-fixes`, `pdf-virus-scan`, `redis-expiration` ni `error-handling` — los tests de verificación de cada cambio deben seguir pasando (actualizando solo los recuentos que este cambio modifica de forma legítima: 116 → 120 nodos y listas nuevas).

## 1. Capa 1 — Migración SQL (reproducible e idempotente)

> Crear `aura-frontend/supabase/migrations/20260814000001_telegram_link_codes_reproducible.sql`. Todos los objetos con sintaxis idempotente (corre en la DB live donde la tabla ya existe y en DBs nuevas). Estilo de referencia: `20260630000002_telegram_link_tokens.sql`.

- [x] 1.1 Crear `CREATE TABLE IF NOT EXISTS public.telegram_link_codes` con el esquema del design D1/D8: `id uuid PK DEFAULT gen_random_uuid()`, `chat_id text NOT NULL`, `code text NOT NULL UNIQUE CHECK (char_length(code) = 6)` (decisión D1: text, no bpchar), `expires_at timestamptz NOT NULL DEFAULT (now() + interval '15 minutes')`, `used_at timestamptz NULL`, `created_at timestamptz NOT NULL DEFAULT now()`
- [x] 1.2 Crear `CREATE INDEX IF NOT EXISTS idx_telegram_link_codes_expires ON public.telegram_link_codes (expires_at)`
- [x] 1.3 `ALTER TABLE public.telegram_link_codes ENABLE ROW LEVEL SECURITY;` sin políticas y sin `FORCE` (decisión D2: únicos consumidores son `service_role` de n8n y el RPC `SECURITY DEFINER`, ambos bypasean RLS; la web nunca lee la tabla directo)
- [x] 1.4 Crear `CREATE OR REPLACE FUNCTION public.link_telegram_with_code(p_code text)` `SECURITY DEFINER SET search_path = public` retornando `json` con el contrato del frontend `{ success, chat_id, error }`: si `auth.uid()` IS NULL → error temprano; `SELECT id, chat_id FROM telegram_link_codes WHERE code = p_code AND used_at IS NULL AND expires_at > now() LIMIT 1`; si no se encuentra → `{ success: false, error: 'Código inválido o expirado.' }`; si se encuentra → `UPDATE profiles SET telegram_chat_id = found.chat_id WHERE id = auth.uid()` (manejar row_count 0 sin marcar el código), `UPDATE telegram_link_codes SET used_at = now() WHERE id = found.id`, y `{ success: true, chat_id: found.chat_id }`
- [x] 1.5 `GRANT EXECUTE ON FUNCTION public.link_telegram_with_code(text) TO authenticated;`
- [x] 1.6 Documentar en el header de la migración: la decisión D1 (text vs bpchar), la decisión D2 (RLS sin políticas), y que `telegram_link_tokens` queda intacta (legacy no usado; limpieza futura fuera de scope)

## 2. Capa 2 — n8n: reconectar `IF - Vinculacion Telegram` y rama de validación

> Editar `codigo.json` con cuidado (116 → 120 nodos). Única arista existente que cambia de destino: `Code in JavaScript7` main[0] pasa de `HTTP - Chequear vinculacion` a `IF - Vinculacion Telegram`. Todo lo demás del bloque `connections` queda intacto. Posiciones propuestas en design D5.

- [x] 2.1 Cambiar la arista de `Code in JavaScript7` main[0] → `IF - Vinculacion Telegram` (design D3, única arista existente modificada)
- [x] 2.2 Agregar entrada de conexión para `IF - Vinculacion Telegram`: main[0] (TRUE = `/start`) → `HTTP - Validar codigo`; main[1] (FALSE = normal) → `HTTP - Chequear vinculacion` (semántica IF: output 0 = true, output 1 = false)
- [x] 2.3 Agregar nodo `HTTP - Validar codigo` (`n8n-nodes-base.httpRequest` v4.4): GET `=https://legffrhakunfignlaftl.supabase.co/rest/v1/telegram_link_codes?select=id&code=eq.{{ $('Telegram Trigger').first().json.message.text.split(' ')[1] }}&used_at=is.null&expires_at=gt.{{ $now.toISO() }}` (expresión D4), credencial `httpHeaderAuth` "Supabase Service Role", `authentication: genericCredentialType`, `sendHeaders: false`, `retryOnFail: true` + `maxTries: 3` + `waitBetweenTries: 1000` (lectura idempotente, política de lecturas), `position: [9040, 102560]`
- [x] 2.4 Agregar nodo `IF - Codigo valido` (`n8n-nodes-base.if` v2.3): condición `={{ $json.length > 0 }}` (respuesta array con items → válido) con formato `conditions`/`operator boolean "true"` idéntico a `IF - Ya vinculado`; sin retry; `position: [9360, 102560]`
- [x] 2.5 Agregar nodo `Telegram - Codigo valido` (`n8n-nodes-base.telegram` v1.2): `chatId ={{ $('Code in JavaScript7').item.json.id_chat }}`, `text "=✅ Código válido. Completá la vinculación desde la web."`, credencial `telegramApi` "Telegram account", sin retry, `webhookId` según formato de export, `position: [9680, 101216]`
- [x] 2.6 Agregar nodo `Telegram - Codigo invalido` (`n8n-nodes-base.telegram` v1.2): `chatId` igual, `text "=❌ Código inválido o expirado."`, credencial `telegramApi` "Telegram account", sin retry, `position: [9680, 102560]`
- [x] 2.7 Agregar aristas nuevas: `HTTP - Validar codigo` main[0] → `IF - Codigo valido`; `IF - Codigo valido` main[0] → `Telegram - Codigo valido`; `IF - Codigo valido` main[1] → `Telegram - Codigo invalido`; y **cero** aristas salientes de los dos nodos Telegram (la rama termina — no debe alimentar `Code - Generar código` ni `HTTP Request1`)
- [x] 2.8 Verificar por inspección que `HTTP - Chequear vinculacion` no cambia sus parámetros (su URL ya referencia `$('Code in JavaScript7')`, que sigue upstream) y que `HTTP Request1` conserva sin retry (D5 de error-handling)

## 3. Capa 3 — Verificación offline (sin stack)

- [x] 3.1 Crear `tests/link-code/verify-link-flow.js` (patrón de `tests/error-handling/verify-retries.js`, sin stack ni contenedores): (a) `codigo.json` parsea; recuento = 120 nodos; `IF - Vinculacion Telegram` tiene arista de entrada desde `Code in JavaScript7` y ambos outputs (`HTTP - Validar codigo` / `HTTP - Chequear vinculacion`); existen `HTTP - Validar codigo` (GET, credencial "Supabase Service Role", retry 3×1000), `IF - Codigo valido`, `Telegram - Codigo valido` y `Telegram - Codigo invalido` (credencial "Telegram account", sin retry); la rama termina (0 aristas salientes de los 2 Telegram); (b) la migración existe y contiene `CREATE TABLE IF NOT EXISTS ... telegram_link_codes`, `CREATE OR REPLACE FUNCTION public.link_telegram_with_code`, `GRANT EXECUTE ... authenticated`; (c) no regresión: 0 secretos (`eyJ`/`Bearer `/`apikey`), cadena ClamAV intacta, retries/credenciales/TTL intactos
- [x] 3.2 Actualizar `tests/error-handling/verify-retries.js`: recuento 116 → 120; agregar `HTTP - Validar codigo` al set de reintentados W1000; los nodos Telegram SEND/edit pasan de 28 a 30 (2 nuevos) sin retry; `supabase.co` pasa de 9 a 10 ocurrencias (URL nueva del nodo de validación)
- [x] 3.3 Actualizar `tests/redis-expiration/verify-ttl.js`: recuento 116 → 120 (el resto de la auditoría Redis — 29 nodos, TTL — no cambia)
- [x] 3.4 Correr `node tests/link-code/verify-link-flow.js`, `node tests/error-handling/verify-retries.js`, `node tests/redis-expiration/verify-ttl.js` y `node tests/rng/test-link-code.js`; confirmar exit 0 en todos

## 4. Pending-manual — despliegue y verificación funcional en el stack

> Requiere acceso a la DB de Supabase (SQL Editor o `supabase db push`) y al stack n8n.

- [x] 4.1 Aplicar `20260814000001_telegram_link_codes_reproducible.sql` en Supabase (SQL Editor o `supabase db push`) — verificado: tabla ya existía en live con `code` como `bpchar` (drift D1 confirmado, no-op preservado), índice `idx_telegram_link_codes_expires` creado, RLS habilitado, RPC creado con `SECURITY DEFINER`. **`profiles.telegram_chat_id` confirmado `text`** — sin cast, D9 resuelto sin drift.
- [x] 4.2 Probar el RPC por SQL: código inexistente (`999999`) → rechazado; código vigente (`ABC123`) → `link_telegram_with_code` devolvió `success: true, chat_id: '123456789'`, `profiles.telegram_chat_id` quedó linkeado y `telegram_link_codes.used_at` marcado (`2026-08-14 18:18:36.177711+00`) — verificado con sesión simulada vía `set_config('request.jwt.claim.sub', ...)`
- [x] 4.3 Re-importado `codigo.json` en n8n: los 4 nodos nuevos aparecen, `IF - Vinculacion Telegram` tiene entrada y ambas salidas correctas. Credencial "Telegram account" matcheó sola. "Supabase Service Role" **no existía todavía en n8n** — se creó de cero (esto destapó que la migración de secretos, change `secrets-migration`, tenía su rotación de claves pendiente; se resolvió en esta misma sesión: JWT legacy filtrado en GitHub desactivado por completo, proyecto migrado al sistema nuevo de Supabase — ver `secrets-migration/tasks.md` §4). Credencial reasignada a los 11 nodos Supabase, incluido `HTTP - Validar codigo`.
- [x] 4.4 Prueba funcional Telegram `/start` — **destapó un bug real en la lógica del branch, corregido en esta sesión**: `IF - Codigo valido` chequeaba `$json.length > 0`, pero el nodo HTTP de n8n separa cada elemento de un array JSON en un ítem individual, así que con un match `$json` es `{ id: "..." }` (objeto, no array) y `.length` da `undefined` → siempre evaluaba "inválido" aunque el código fuera correcto. Fix: `HTTP - Validar codigo` con `alwaysOutputData: true` (si no, con 0 matches el nodo no emite ningún ítem y el flujo no dispara ningún mensaje) + condición del IF cambiada a `{{ !!$json.id }}`. Aplicado en `codigo.json` y en vivo en n8n. Verificado con código válido (`/start ZZZ111` → "✅ Código válido...") y código inexistente (`/start NOEXISTE` → "❌ Código inválido o expirado."). Verificado también que un mensaje normal ("hola") sigue el flujo de siempre sin interferencia — confirmado que `IF - Vinculacion Telegram` usa `startsWith("/start ")` con espacio, así que el botón nativo "Start" de Telegram (manda `/start` sin espacio ni código) NO entra a esta rama y sigue el flujo original de generación de código (`Code - Generar código`, que le pide al usuario pegar el código en la web, no en Telegram). La rama `/start <código>` es un camino secundario (red de contención / preparado para un futuro deep-link `t.me/bot?start=<código>`, ver `proposal.md` — `telegram_link_tokens` fue un intento de deep-link nunca completado).
- [x] 4.5 Prueba funcional de expiración: código insertado con `expires_at = now() - interval '1 minute'` (bpchar 6 — `EXPIR1`); `/start EXPIR1` → "❌ Código inválido o expirado." (bot). Verificación del RPC web con código expirado no repetida por separado — la query de `link_telegram_with_code` usa el mismo filtro `expires_at > now()` ya validado en la 4.2, comportamiento idéntico esperado.
- [x] 4.6 Prueba funcional web RPC: en `ConnectionsPage`, código vigente vinculó la cuenta con éxito. Nota: el input de la web solo acepta dígitos (coherente con el generador real, `randomInt(100000, 1000000)` — códigos alfanuméricos de prueba como `WEBTST` no pasan la validación del campo aunque sí cumplan la constraint de la DB; hay que usar códigos numéricos de 6 dígitos para probar vía UI).
- [x] 4.7 Re-exportado el workflow desde n8n. El export real destapó diferencias de formato entre el edit manual original y lo que n8n serializa de verdad (no regresiones, solo desactualizaba los tests): `sendHeaders:false` se omite al exportar (default), y `onError` de `Escaneo ClamAV` es propiedad de nivel superior del nodo, no de `parameters` — ambos ajustados en `verify-link-flow.js`/`verify-retries.js`. Sí hubo una regresión real: `Escaneo ClamAV` perdió su `onError: continueErrorOutput` durante las ediciones en vivo de hoy — reactivado en n8n y confirmado en el re-export. También se sanearon 9 ids reales de credencial (`httpHeaderAuth`) que el export trajo en texto plano, reemplazados por `<placeholder>` (repo es público). Los 4 tests offline pasan, salvo 1 falla esperada (2 nodos Postiz sin credencial — pendiente, no bloquea este change).
- [x] 4.8 `HALLAZGOS-DEL-FLUJO-n8n.md` actualizado: §2.8 (estado remediado completo, incluida la respuesta a la pregunta de convivencia de tablas), N.3 y N.6 marcadas como respondidas, y nueva §0.5 documentando el cierre del incidente `service_role` (repo confirmado público, legacy keys desactivadas, Postiz y purga de historial de Git como pendientes).
